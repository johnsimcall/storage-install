---
- name: Create a volume group on top of {{ disk }}
  lvg:
    vg: "vg_{{ disk }}"
    pvs: "/dev//mapper/vdo-{{ disk }}1"

- name: Create a logical volume the size of all remaining space in the volume group
  lvol:
    vg: "vg_{{ disk }}"
    lv: "lv_{{ disk }}"
    size: 100%FREE
